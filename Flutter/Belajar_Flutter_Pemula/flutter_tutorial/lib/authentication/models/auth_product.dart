class AuthProduct {
  String id, title, price;
  DateTime createdAt, updatedAt;

  AuthProduct(
      {required this.id,
      required this.title,
      required this.price,
      required this.createdAt,
      required this.updatedAt});
}
