class Player {
  String id, name, position, imageUrl;
  DateTime createdAt;

  Player(
      {required this.id,
      required this.name,
      required this.position,
      required this.imageUrl,
      required this.createdAt});
}
