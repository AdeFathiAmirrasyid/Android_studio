import 'package:flutter/material.dart';

class HomePageGetStorage extends StatelessWidget {
  const HomePageGetStorage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Get Storage"),),
    );
  }
}
